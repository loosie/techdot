spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/techdot_dev
    username: test_user
    password: .
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
